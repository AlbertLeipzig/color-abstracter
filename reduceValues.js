const reduceValues = (valuesMap) => {
    const rawValuesObject = Object.fromEntries(valuesMap)
    const editedValuesObject = {...rawValuesObject}

    for (const value of rawValuesObject){
        if (value !== 0) {
            
        }
    }



    return editedValuesObject
};

export default reduceValues;
