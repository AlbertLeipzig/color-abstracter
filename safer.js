/* const convertSetToMap = (_set, tag) => {
  let map = new Map();
  const sortedArrayFromSet = Array.from(_set).sort();

  sortedArrayFromSet.forEach((value) => {
    map.set(`${tag}`, `${value}`);
  });
  return map;
};

 */